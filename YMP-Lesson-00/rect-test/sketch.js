function setup() {
  // setup of the sketch
  createCanvas(400, 400);
  background(51);
}

function draw() {
  // here you draw to the screen
  rect(30, 20, 55, 55);
  triangle(30, 75, 58, 20, 86, 75);
}